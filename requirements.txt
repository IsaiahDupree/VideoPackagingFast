# Core dependencies
PySimpleGUI>=4.60.0
moviepy==1.0.3
pydub==0.25.1
python-dotenv==1.0.0
pillow==10.2.0
ffmpeg-python==0.2.0
numpy>=1.22.0
tqdm>=4.64.0
requests>=2.28.0
packaging>=23.0

# AI API dependencies
openai==1.12.0
anthropic==0.8.1

# Optional dependencies (comment out if causing issues)
# torch>=2.0.0  # Only needed for local Whisper transcription
# openai-whisper==20231117  # Only needed for local Whisper transcription

# Build tools
PyInstaller==5.6.2
